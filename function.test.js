const {
    group
} = require('console');
const db = require('./query');
// const app = require('./index');


// describe('API', () => {


//     it('GET /users --> items', () => {
//         return request(app).get('/items').expect('content-Type', /json/).expect(200).then((response) => {
//             expect(response.body).toEqual(expect.arrayContaining([
//                 entity = expect.any(string),
//                 details = expect.any(string)
//             ]))
//         });

//     });

// });
describe("group", () => {

test('add 1 + 2 to equal 3', async () => {
    expect(1 + 2).toBe(3);

})
test('add 1 + 2 to equal 3', async () => {
    expect(1 + 2).toBe(3);

})
})


